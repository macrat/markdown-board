# Environment Variables for Markdown Board

# Next.js Public Environment Variables
# Port for the WebSocket server used by the client (default: 1234)
NEXT_PUBLIC_WS_PORT=1234

# Yjs Persistence Configuration
# Directory path for storing Yjs documents (enables LevelDB persistence)
# If not set, documents are stored in memory only (not persistent across restarts)
YPERSISTENCE=.yjs

# Cleanup Script Configuration
# Base URL for the API server used by cleanup-archives.js script (default: http://localhost:3000)
API_URL=http://localhost:3000

# Next.js Environment
# Set to 'production' for production builds (automatically set by Next.js)
# NODE_ENV=production

# CI Environment
# Set to 'true' when running in CI environment (used by Playwright tests)
# CI=true
